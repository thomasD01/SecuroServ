
const isDev = process.env.NODE_ENV !== 'production'

const serverURL = isDev ? 'localhost:3001': '';

export async function getBusinesses(userID: string){

}
export async function getBusinessData(businessID: string){
  
}